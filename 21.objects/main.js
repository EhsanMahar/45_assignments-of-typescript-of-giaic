"use strict";
//create two objects
const book = {
    name: "essental typescript",
    price: 200
};
const apple = {
    name: "apple",
    price: 200,
};
console.log(`book name: ${book.name},price: $${book.price}`);
console.log(`apple name: ${apple.name},price: $${apple.price}`);
